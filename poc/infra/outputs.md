# Infrastructure Outputs

After deployment, capture:

- Broker Container App URL
- Revenue MCP Container App URL
- Log Analytics workspace name
- Container Apps Environment name

Use these to populate service runtime configuration and E2E test scripts.
